// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "./registrazione.sol";
import "./interfaces.sol";


contract Login{
    
    // function controlloCredenziali(address _contractAddress,address private_key_sender) external view returns(bool) {   //per ora gli passo la chiave pubblica 
       
    //     Utente [] memory lista_utenti;
    //     uint i;
        
    //     lista_utenti=reg(_contractAddress).get_registrati();
        
    //     for(i=0;i<lista_utenti.length;i++) {
            
    //       if (lista_utenti[i].id == private_key_sender){     // in seguito diventa lista_utenti[i].id==chiave pubblica associata a private_key_sender
    //           return true;
    //       }
    //     }
        
    //     return false;
        
    // }
}